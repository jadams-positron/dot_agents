interface:
  display_name: "Work GitHub Issues"
  short_description: "Launch isolated issue and stack owners"
  default_prompt: "Use $work-gh-issues to find actionable issues, map their dependencies and write-set overlap, and launch one Agent Deck owner per independent issue or native GitHub PR stack."
